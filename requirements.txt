# ============================================================
# MedBoard — requirements.txt
# Python 3.10+
#
# IMPORTANT — PyTorch CUDA Install (run this FIRST, separately):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   (cu124 is required for Python 3.13 — cu121 does NOT have Python 3.13 wheels)
#
# Then install the rest:
#   pip install -r requirements.txt
# ============================================================

# --- Deep Learning ---
# torch and torchvision installed separately via CUDA wheel (see above)
timm>=1.0.9                        # EfficientNet-B0 and other pretrained models
monai>=1.4.0                       # Medical imaging transforms and utilities

# --- Agent Orchestration ---
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0      # LangChain wrapper for Gemini

# --- LLM / Gemini ---
google-generativeai>=0.8.0

# --- RAG / Embeddings ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Image Processing ---
Pillow>=10.0.0
opencv-python>=4.10.0
scikit-image>=0.24.0

# --- Scientific Computing ---
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0

# --- Config & Env ---
pyyaml>=6.0.1
python-dotenv>=1.0.0

# --- UI ---
streamlit>=1.40.0
plotly>=5.24.0                     # Interactive charts in Streamlit

# --- Utilities ---
tqdm>=4.66.0
matplotlib>=3.9.0
kaggle>=1.6.0                      # Kaggle API for dataset download
