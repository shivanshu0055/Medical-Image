# ============================================================
# MedBoard — requirements.txt
# Python 3.13 | PyTorch 2.6.0+cu124 | RTX 2050 4GB
#
# Install order (MUST follow this order):
#   Step 1 — PyTorch CUDA (from pytorch.org, NOT PyPI):
#     pip install torch==2.6.0+cu124 torchvision==0.21.0+cu124 --index-url https://download.pytorch.org/whl/cu124
#
#   Step 2 — Everything else:
#     pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124
#
# The --extra-index-url flag below tells pip to prefer the cu124 index for
# torch/torchvision so they are never replaced by CPU builds from PyPI.
# ============================================================

# --- Deep Learning (pinned to CUDA builds) ---
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
timm>=1.0.9                        # EfficientNet-B0 and other pretrained models
# monai REMOVED — designed for 3D NIfTI volumes; overkill for our 2D .jpg pipeline.
# torchvision.transforms + scikit-image covers all our 2D preprocessing needs.

# --- Agent Orchestration ---
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0      # LangChain wrapper for Gemini

# --- LLM / Gemini ---
google-generativeai>=0.8.0

# --- RAG / Embeddings ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- Image Processing ---
Pillow>=10.0.0
opencv-python>=4.10.0
scikit-image>=0.24.0

# --- Scientific Computing ---
numpy>=1.26.0
scipy>=1.13.0
pandas>=2.2.0

# --- Config & Env ---
pyyaml>=6.0.1
python-dotenv>=1.0.0

# --- UI ---
streamlit>=1.40.0
plotly>=5.24.0                     # Interactive charts in Streamlit

# --- Utilities ---
tqdm>=4.66.0
matplotlib>=3.9.0
kaggle>=1.6.0                      # Kaggle API for dataset download
