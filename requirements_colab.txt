# ============================================================
# MedBoard — requirements_colab.txt
# Use this in Google Colab cells instead of requirements.txt
#
# Run at the top of any Colab notebook:
#   !pip install -q -r requirements_colab.txt
#
# PyTorch + torchvision are pre-installed in Colab with CUDA.
# Do NOT reinstall torch here — Colab's version is fine.
#
# google-auth and google-generativeai are pre-installed in Colab.
# Do NOT pin them here — Colab's resolver handles them internally.
# Any version conflict warnings from pip are harmless.
# ============================================================

timm>=1.0.9
# monai REMOVED — see requirements.txt for explanation
langchain>=0.3.0
langgraph>=0.2.0
langchain-google-genai>=2.0.0
# google-generativeai: installed as a dependency of langchain-google-genai
# google-auth: let Colab manage this — pinning causes conflicts
chromadb>=0.5.0
sentence-transformers>=3.0.0
scikit-image>=0.24.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
plotly>=5.24.0
kaggle>=1.6.0
